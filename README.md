# Notes App API

This Notes App API is a full-stack application built with React on the frontened and Node.js/Express on the backend. It allows userts to create, edit, view and delete notes.

# Technologies 🚀
- Frontend: [REACT](https://react.dev/) version 19.1.1.
- Backend: Node.js + Express.

## Development server backend 🛠️

- Use `cd backend` and run `npm install` to install dependencies.
- Run `npm start dev`. The app will automatically reload if you change any source files.

## Development server frontend 🖥️

- Use command `cd frontend` and run `npm install` to install dependencies.
- Run `npm run dev` and navigate to `http://localhost:5173/`. The app will automatically reload if you change any source files.

## Features ✨

- Create, edit, view and delete notes.
- Clean and organized interface to visualize all notes.
- Responsive layout for desktop and mobile.
- Modular structure separating frontend and backend
